import React from "react";
import title from "../img/title.png";

const Header = () => {
  return (
    <header className="center">
      <img src={title} alt="" />
    </header>
  );
};

export default Header;
